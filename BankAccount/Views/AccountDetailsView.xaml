<UserControl x:Class="Bank.Views.AccountDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bank.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <StackPanel Grid.Row="0">
      <TextBox x:Name="txtFirstName" Text="First Name" Width="100" HorizontalAlignment="Left"/>
    </StackPanel>
    <StackPanel Grid.Row="1">
      <TextBox x:Name="txtLastName" Text="Last Name" Width="100" HorizontalAlignment="Left"/>
    </StackPanel>
    <StackPanel Grid.Row="2">
      <Button x:Name="btnNext" Command="{Binding Path=NextCommand}" Content="Next" Width="100" HorizontalAlignment="Left"/>
    </StackPanel>
  </Grid>
</UserControl>
